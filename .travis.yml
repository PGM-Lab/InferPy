language: python
python:
  - "3.5"
  - "3.6"

matrix:
  include:
    - python: 3.5
      env: TOXENV=py35
    - python: 3.6
      env: TOXENV=py35

# command to install dependencies
install:
  - pip install tox
# command to run tests
script:
  - TOX_SKIP_MISSING_INTERPRETERS="False"; tox